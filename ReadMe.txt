To run the project you will have to run the script ProductScript.sql, which will create the product table and populate it with data, 
then change the Web.config connection string "SpecialityDrinksConnectionString2" to the server details of database, which you ran
the sql script on.

Thanks.